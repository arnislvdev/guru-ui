export interface ChartCoreOptions {
  // Add options based on component props
}

export class ChartCore {
  static getClasses(options: ChartCoreOptions): string {
    // Extract classes from React component
    return ''
  }

  static getAccessibilityProps(options: ChartCoreOptions) {
    // Extract accessibility props
    return {}
  }

  static getDefaultOptions(): ChartCoreOptions {
    return {}
  }
}
