export interface ProgressCircleCoreOptions {
  // Add options based on component props
}

export class ProgressCircleCore {
  static getClasses(options: ProgressCircleCoreOptions): string {
    // Extract classes from React component
    return ''
  }

  static getAccessibilityProps(options: ProgressCircleCoreOptions) {
    // Extract accessibility props
    return {}
  }

  static getDefaultOptions(): ProgressCircleCoreOptions {
    return {}
  }
}
