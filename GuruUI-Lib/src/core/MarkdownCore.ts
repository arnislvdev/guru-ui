export interface MarkdownCoreOptions {
  // Add options based on component props
}

export class MarkdownCore {
  static getClasses(options: MarkdownCoreOptions): string {
    // Extract classes from React component
    return ''
  }

  static getAccessibilityProps(options: MarkdownCoreOptions) {
    // Extract accessibility props
    return {}
  }

  static getDefaultOptions(): MarkdownCoreOptions {
    return {}
  }
}
